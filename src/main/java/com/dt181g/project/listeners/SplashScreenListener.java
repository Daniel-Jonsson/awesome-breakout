package com.dt181g.project.listeners;

/**
 * The {@code SetupScreenListener} is used to receive updates on changes and
 * act accordingly
 * @author Daniel Jönsson
 * @version 1.0
 */
public interface SplashScreenListener {

    /**
     * The single method onComplete() is used to notify
     */
    void onComplete();
}
