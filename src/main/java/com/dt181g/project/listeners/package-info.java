/**
 * Listener classes used to listen for updates in view during
 * splashscreen and setup. The
 * {@link com.dt181g.project.controllers.SplashScreenController} implements
 * the {@code SplashScreenListener} and overrides its single method
 * onComplete(). {@link com.dt181g.project.controllers.SetupScreenController}
 * on the other hand implements the {@code SetupScreenListener} and overrides
 * its single method onStart().
 * @author Daniel Jönsson
 * @version 1.0
 */
package com.dt181g.project.listeners;